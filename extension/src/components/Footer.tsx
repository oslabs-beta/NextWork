import "../styles.css";
//import sub-components

const Footer = (/*props*/) => {
  //const blah = React.useState(0);
  //React.useEffect(() => {set some state})
  return <footer className={"footer"}>possibly pass some state</footer>;
};

export default Footer;
